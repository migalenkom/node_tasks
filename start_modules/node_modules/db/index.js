var translation;
exports.connect = function() {
  translation = require('./ru')
};


exports.getTranslation = function(name) {
  if (!translation[name]) {
    throw new Error('not found: ' + name);
  }
  return translation[name];
};
